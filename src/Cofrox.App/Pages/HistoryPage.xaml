<Page
    x:Class="Cofrox.App.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="Refresh" Label="Refresh" Command="{Binding LoadCommand}" />
            <AppBarButton Icon="Delete" Label="Clear History" Command="{Binding ClearCommand}" />
        </CommandBar>

        <Grid Grid.Row="1">
            <ProgressRing Width="42"
                          Height="42"
                          IsActive="{Binding IsLoading}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center" />

            <ListView ItemsSource="{Binding Entries}"
                      Visibility="{Binding Entries.Count, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,12"
                                Padding="18"
                                CornerRadius="20"
                                Background="{ThemeResource CofroxCardBrush}">
                            <StackPanel Spacing="6">
                                <TextBlock Text="{Binding FileName}" />
                                <TextBlock Style="{StaticResource MonoLabelStyle}"
                                           Text="{Binding SourceFormat}" />
                                <TextBlock Style="{StaticResource MonoLabelStyle}"
                                           Text="{Binding TargetFormat}" />
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Text="{Binding ConvertedAt}" />
                                <TextBlock Foreground="{StaticResource CofroxAccentBrush}"
                                           Text="{Binding Status}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="10"
                        Visibility="{Binding Entries.Count, Converter={StaticResource BoolToVisibilityConverter}}">
                <FontIcon Glyph="&#xE81C;" FontSize="42" />
                <TextBlock Style="{StaticResource SectionHeadingStyle}" Text="No conversions yet. Drop a file to start." />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
