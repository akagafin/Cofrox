<Page
    x:Class="Cofrox.App.Views.Settings.LicensesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Cofrox.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:models="using:Cofrox.App.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:LegalBadgeKindToBrushConverter x:Key="LegalBadgeKindToBrushConverter" />
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton x:Name="BackButton" Icon="Back" Click="BackButton_Click" />
            <AppBarButton x:Name="CopyButton" Icon="Copy" Command="{Binding CopyAllNoticesCommand}" />
        </CommandBar>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="24" Spacing="16">
                <TextBlock Text="{Binding IntroText}"
                           TextWrapping="WrapWholeWords"
                           IsTextSelectionEnabled="True" />

                <ItemsControl ItemsSource="{Binding Notices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:LegalLibraryNotice">
                            <Expander>
                                <Expander.Header>
                                    <Grid ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Spacing="2">
                                            <TextBlock Text="{Binding Name}" />
                                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                       Text="{Binding Version}" />
                                        </StackPanel>

                                        <Border Grid.Column="1"
                                                Padding="10,4"
                                                CornerRadius="999"
                                                Background="{Binding BadgeKind, Converter={StaticResource LegalBadgeKindToBrushConverter}}"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding LicenseType}" />
                                        </Border>
                                    </Grid>
                                </Expander.Header>

                                <StackPanel Spacing="8" Margin="0,12,0,0">
                                    <TextBlock Text="{Binding Copyright}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               IsTextSelectionEnabled="True"
                                               TextWrapping="WrapWholeWords" />

                                    <TextBlock Text="{Binding Note}"
                                               Visibility="{Binding HasNote, Converter={StaticResource BoolToVisibilityConverter}}"
                                               IsTextSelectionEnabled="True"
                                               TextWrapping="WrapWholeWords" />

                                    <ScrollViewer MaxHeight="240"
                                                  VerticalScrollBarVisibility="Auto">
                                        <TextBlock Text="{Binding LicenseText}"
                                                   FontFamily="Cascadia Code"
                                                   FontSize="12"
                                                   IsTextSelectionEnabled="True"
                                                   TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </StackPanel>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
