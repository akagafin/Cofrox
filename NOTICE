This repository contains Cofrox application code under the MIT License.

Cofrox also redistributes or interoperates with third-party software. See
THIRD_PARTY_LICENSES.txt for the full inventory, license analysis, and
redistribution obligations.

Important notices for the default app build and the optional local tool bundle:

1. FFmpeg
   The default `Portable` publish profile does not ship FFmpeg. If you add
   FFmpeg through the optional local `Tools` folder, the developer download
   script currently pulls a build compiled with --enable-gpl and
   --enable-version3, and with GPL-triggering libraries such as libx264 and
   libx265. That binary is not an LGPL-only build. If you redistribute that
   exact binary, you must satisfy the GPLv3 redistribution requirements for the
   FFmpeg component, including providing the exact corresponding source and
   build information for that binary.

2. Pandoc
   The default `Portable` publish profile does not ship Pandoc. If you add
   Pandoc through the optional local `Tools` folder, preserve its notices and
   make the corresponding source code for the exact redistributed version
   available in the distribution workflow.

3. ImageMagick
   If ImageMagick is bundled through the optional local `Tools` folder, keep
   its LICENSE.txt and NOTICE.txt files in any packaged distribution and
   preserve attribution to ImageMagick Studio LLC.

4. 7-Zip
   If 7-Zip is bundled through the optional local `Tools` folder, preserve the
   upstream license information and any unRAR restriction notice with
   redistribution.

5. Optional external integrations
   Ghostscript and LibreOffice are referenced by the codebase as optional
   external tools, but they are not bundled in the current repository tree.
   Do not redistribute them from Cofrox without separately carrying forward the
   exact notices and license terms from the chosen upstream package.
